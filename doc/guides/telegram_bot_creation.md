# Создание Telegram-бота с помощью BotFather

Данная инструкция поможет вам создать нового Telegram-бота для проекта LLM-ассистента подбора аниме.

## Что такое BotFather?

BotFather — это официальный бот Telegram для создания и управления другими ботами. Он работает как команда для настройки токенов, имен, описаний и других параметров ваших ботов.

## Пошаговая инструкция

### Шаг 1: Найти BotFather

1. Откройте Telegram (мобильное приложение или десктоп)
2. В поиске введите `@BotFather` или перейдите по ссылке: https://t.me/BotFather
3. Нажмите "Start" или отправьте команду `/start`

### Шаг 2: Создать нового бота

1. Отправьте команду `/newbot` боту BotFather
2. BotFather попросит ввести имя для вашего бота:
   ```
   Alright, a new bot. How are we going to call it? 
   Please choose a name for your bot.
   ```
   
3. Введите имя бота, например: `Аниме Сайтама Бот`
   
4. Далее BotFather попросит ввести username (имя пользователя) для бота:
   ```
   Good. Now let's choose a username for your bot. 
   It must end in `bot`. Like this, for example: TetrisBot or tetris_bot.
   ```

5. Введите уникальное имя пользователя, которое должно заканчиваться на `bot`, например:
   - `anime_saitama_bot`
   - `animerecommender_bot`
   - `saitama_anime_bot`

### Шаг 3: Получить токен

После успешного создания бота, BotFather пришлет вам сообщение с токеном:

```
Done! Congratulations on your new bot. You will find it at t.me/anime_saitama_bot. 
You can now add a description, about section and profile picture for your bot, see /help for a list of commands. 

Use this token to access the HTTP API:
1234567890:AABBCCDDeeFFggHHiiJJkkLLmmNNooP

Keep your token secure and store it safely, it can be used by anyone to control your bot.
```

**ВАЖНО:** Токен должен выглядеть примерно так: `1234567890:AABBCCDDeeFFggHHiiJJkkLLmmNNooP`

### Шаг 4: Настроить бота (опционально)

#### Установить описание
```
/setdescription
```
Пример описания:
```
🎌 Привет! Я Сайтама, и теперь помогаю подбирать аниме. 

Не самая захватывающая работа, но ладно... Расскажи мне, что хочешь посмотреть, и я найду что-нибудь подходящее.

Используй /start чтобы начать или просто напиши, какое аниме ищешь.
```

#### Установить краткое описание
```
/setabouttext
```
Пример:
```
ИИ-ассистент для подбора аниме в стиле Сайтамы из "Ванпанчмен". Получай персональные рекомендации аниме!
```

#### Установить команды
```
/setcommands
```
Введите список команд:
```
start - Начать работу с ботом
help - Получить справку
top - Популярные аниме
new - Новинки сезона
classic - Классические аниме
reset - Сбросить контекст диалога
```

#### Установить аватар (опционально)
```
/setuserpic
```
Загрузите изображение Сайтамы или аниме-тематики.

## Настройка токена в проекте

### Шаг 1: Создать файл переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте файл `.env` в текстовом редакторе

3. Найдите строку с `TELEGRAM_BOT_TOKEN` и вставьте ваш токен:
   ```bash
   TELEGRAM_BOT_TOKEN=1234567890:AABBCCDDeeFFggHHiiJJkkLLmmNNooP
   ```

### Шаг 2: Проверить настройки

Убедитесь, что файл `.env` содержит все необходимые переменные:

```bash
# Обязательные переменные
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
OPENROUTER_API_KEY=ваш_ключ_от_openrouter

# Опциональные переменные (с значениями по умолчанию)
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free
CACHE_TTL_HOURS=24
LOG_LEVEL=INFO
```

### Шаг 3: Проверить работу

1. Запустите бота локально:
   ```bash
   uv run src/bot.py
   ```

2. Найдите вашего бота в Telegram по username
3. Отправьте команду `/start`
4. Убедитесь, что бот отвечает

## Безопасность токена

### ⚠️ ВАЖНЫЕ ПРАВИЛА БЕЗОПАСНОСТИ:

1. **Никогда не публикуйте токен в открытых репозиториях**
2. **Добавьте `.env` в `.gitignore`**
3. **Не отправляйте токен в сообщениях**
4. **Используйте переменные окружения на сервере**

### Если токен скомпрометирован:

1. Откройте BotFather
2. Отправьте `/revoke` 
3. Выберите ваш бот
4. Получите новый токен
5. Обновите `.env` файл

## Полезные команды BotFather

- `/mybots` - список ваших ботов
- `/setname` - изменить имя бота
- `/setdescription` - изменить описание
- `/setabouttext` - изменить краткое описание
- `/setuserpic` - изменить аватар
- `/setcommands` - настроить список команд
- `/deletebot` - удалить бота
- `/revoke` - получить новый токен (старый перестанет работать)

## Дополнительные настройки

### Режим приватности групп
```
/setprivacy
```
- **Enabled** - бот видит только сообщения, которые начинаются с `/` или содержат @username бота
- **Disabled** - бот видит все сообщения в группе

Для нашего бота рекомендуется **Enabled** (режим по умолчанию).

### Режим встроенных запросов
```
/setinline
```
Позволяет пользователям упоминать бота в любом чате через `@botname`. 
Для нашего проекта это опционально.

## Тестирование бота

После создания обязательно протестируйте:

1. **Команды:**
   - `/start` - приветствие работает
   - `/help` - справка отображается
   - Текстовые сообщения - бот отвечает в стиле Сайтамы

2. **Разные типы чатов:**
   - Личные сообщения
   - Группы (если планируется)

3. **Ошибки:**
   - Неизвестные команды
   - Слишком длинные сообщения
   - Сетевые сбои

## Часто задаваемые вопросы

**Q: Сколько ботов можно создать?**
A: Telegram не ограничивает количество ботов на аккаунт.

**Q: Можно ли изменить username бота?**
A: Нет, username нельзя изменить. Придется создавать нового бота.

**Q: Бот не отвечает, что делать?**
A: Проверьте:
- Правильность токена в `.env`
- Запущен ли процесс бота
- Нет ли ошибок в логах

**Q: Можно ли использовать одного бота для разработки и продакшена?**
A: Лучше создать отдельных ботов для dev и prod окружений.

---

**Готово!** Теперь у вас есть Telegram-бот, готовый для интеграции с LLM-ассистентом Сайтамы. 🎌

Переходите к следующему этапу разработки согласно `doc/tasklist.md`.
